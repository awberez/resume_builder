<main>
    <div class="container">
        <div class="containerCard">
            <div class="row">
                <div class="col s12">
                    <h5>Choose a tag to build your custom resume!</h5>
                    {{#each tags}}  
                    <a href="/resume/{{this.id}}/{{this.tag}}" class="waves-effect waves-light light-blue btn-large">{{this.tag}}</a>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
    <script type="text/javascript">
    $(document).ready(function() {
        $("#buildTab").addClass("active");
    });
    </script>
</main>